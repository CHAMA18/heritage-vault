"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const jwt_js_1 = require("./jwt.js");
const workloadDeploymentToken_js_1 = require("./workloadDeploymentToken.js");
const SECRET = "test-workload-token-secret";
const EXP = Math.floor(Date.UTC(2032, 0, 1) / 1000);
const claims = {
    deployment: "deployment_abc123",
    deployment_version: "20260709.1",
    environment_id: "env_1",
    environment_type: "PRODUCTION",
    org_id: "org_1",
    project_id: "proj_1",
};
(0, vitest_1.describe)("workloadDeploymentToken", () => {
    (0, vitest_1.it)("mints a token that verifies and round-trips every claim", async () => {
        const token = await (0, workloadDeploymentToken_js_1.mintWorkloadDeploymentToken)(claims, SECRET, EXP);
        const result = await (0, workloadDeploymentToken_js_1.verifyWorkloadDeploymentToken)(token, SECRET);
        (0, vitest_1.expect)(result.ok).toBe(true);
        if (!result.ok)
            return;
        (0, vitest_1.expect)(result.claims.ver).toBe(workloadDeploymentToken_js_1.WORKLOAD_DEPLOYMENT_TOKEN_VERSION);
        (0, vitest_1.expect)(result.claims.deployment).toBe(claims.deployment);
        (0, vitest_1.expect)(result.claims.deployment_version).toBe(claims.deployment_version);
        (0, vitest_1.expect)(result.claims.environment_id).toBe(claims.environment_id);
        (0, vitest_1.expect)(result.claims.environment_type).toBe(claims.environment_type);
        (0, vitest_1.expect)(result.claims.org_id).toBe(claims.org_id);
        (0, vitest_1.expect)(result.claims.project_id).toBe(claims.project_id);
    });
    (0, vitest_1.it)("sets the caller-supplied absolute exp", async () => {
        const token = await (0, workloadDeploymentToken_js_1.mintWorkloadDeploymentToken)(claims, SECRET, EXP);
        const result = await (0, workloadDeploymentToken_js_1.verifyWorkloadDeploymentToken)(token, SECRET);
        (0, vitest_1.expect)(result.ok).toBe(true);
        if (!result.ok)
            return;
        (0, vitest_1.expect)(result.claims.exp).toBe(EXP);
    });
    (0, vitest_1.it)("mints byte-identical tokens for identical claims (deterministic, no iat)", async () => {
        const a = await (0, workloadDeploymentToken_js_1.mintWorkloadDeploymentToken)(claims, SECRET, EXP);
        const b = await (0, workloadDeploymentToken_js_1.mintWorkloadDeploymentToken)(claims, SECRET, EXP);
        (0, vitest_1.expect)(a).toBe(b);
        const result = await (0, workloadDeploymentToken_js_1.verifyWorkloadDeploymentToken)(a, SECRET);
        (0, vitest_1.expect)(result.ok).toBe(true);
        if (!result.ok)
            return;
        (0, vitest_1.expect)(result.claims.iat).toBeUndefined();
    });
    (0, vitest_1.it)("rejects a token signed with a different secret", async () => {
        const token = await (0, workloadDeploymentToken_js_1.mintWorkloadDeploymentToken)(claims, SECRET, EXP);
        const result = await (0, workloadDeploymentToken_js_1.verifyWorkloadDeploymentToken)(token, "wrong-secret");
        (0, vitest_1.expect)(result.ok).toBe(false);
        if (result.ok)
            return;
        (0, vitest_1.expect)(result.reason).toBe("invalid_signature");
    });
    (0, vitest_1.it)("rejects a tampered token", async () => {
        const token = await (0, workloadDeploymentToken_js_1.mintWorkloadDeploymentToken)(claims, SECRET, EXP);
        const [header, payload, signature] = token.split(".");
        const tampered = `${header}.${payload}x.${signature}`;
        const result = await (0, workloadDeploymentToken_js_1.verifyWorkloadDeploymentToken)(tampered, SECRET);
        (0, vitest_1.expect)(result.ok).toBe(false);
    });
    (0, vitest_1.it)("rejects a valid JWT that is missing required claims", async () => {
        // Signed with the right secret, but not a workload deployment token.
        const token = await (0, jwt_js_1.generateJWT)({
            secretKey: SECRET,
            payload: { foo: "bar" },
            expirationTime: "1825d",
        });
        const result = await (0, workloadDeploymentToken_js_1.verifyWorkloadDeploymentToken)(token, SECRET);
        (0, vitest_1.expect)(result.ok).toBe(false);
        if (result.ok)
            return;
        (0, vitest_1.expect)(result.reason).toBe("malformed_claims");
    });
    (0, vitest_1.describe)("looksLikeWorkloadDeploymentToken", () => {
        (0, vitest_1.it)("is true for a minted token", async () => {
            const token = await (0, workloadDeploymentToken_js_1.mintWorkloadDeploymentToken)(claims, SECRET, EXP);
            (0, vitest_1.expect)((0, workloadDeploymentToken_js_1.looksLikeWorkloadDeploymentToken)(token)).toBe(true);
        });
        (0, vitest_1.it)("is false for a legacy bare friendlyId", () => {
            (0, vitest_1.expect)((0, workloadDeploymentToken_js_1.looksLikeWorkloadDeploymentToken)("deployment_abc123")).toBe(false);
        });
        (0, vitest_1.it)("is false for empty and malformed values", () => {
            (0, vitest_1.expect)((0, workloadDeploymentToken_js_1.looksLikeWorkloadDeploymentToken)("")).toBe(false);
            (0, vitest_1.expect)((0, workloadDeploymentToken_js_1.looksLikeWorkloadDeploymentToken)("a.b")).toBe(false);
            (0, vitest_1.expect)((0, workloadDeploymentToken_js_1.looksLikeWorkloadDeploymentToken)("a..c")).toBe(false);
        });
    });
    (0, vitest_1.describe)("classifyDeploymentIdHeader", () => {
        (0, vitest_1.it)("classifies a minted token as jwt_valid and returns claims", async () => {
            const token = await (0, workloadDeploymentToken_js_1.mintWorkloadDeploymentToken)(claims, SECRET, EXP);
            const result = await (0, workloadDeploymentToken_js_1.classifyDeploymentIdHeader)(token, SECRET);
            (0, vitest_1.expect)(result.outcome).toBe("jwt_valid");
            (0, vitest_1.expect)(result.claims?.deployment).toBe(claims.deployment);
        });
        (0, vitest_1.it)("classifies a JWT with a bad signature as jwt_invalid", async () => {
            const token = await (0, workloadDeploymentToken_js_1.mintWorkloadDeploymentToken)(claims, SECRET, EXP);
            const result = await (0, workloadDeploymentToken_js_1.classifyDeploymentIdHeader)(token, "wrong-secret");
            (0, vitest_1.expect)(result.outcome).toBe("jwt_invalid");
            (0, vitest_1.expect)(result.claims).toBeUndefined();
        });
        (0, vitest_1.it)("classifies a bare friendlyId as legacy_bare", async () => {
            const result = await (0, workloadDeploymentToken_js_1.classifyDeploymentIdHeader)("deployment_abc123", SECRET);
            (0, vitest_1.expect)(result.outcome).toBe("legacy_bare");
        });
    });
});
//# sourceMappingURL=workloadDeploymentToken.test.js.map