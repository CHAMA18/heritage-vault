# HeritageAtlas — live credentials (NEVER commit this file)
# Added 2026-07-23 after provisioning ClickHouse Cloud

# ── ClickHouse (primary database — LIVE) ─────────────────────────────
CLICKHOUSE_URL=https://fgkcf51umc.eu-central-1.aws.clickhouse.cloud:8443
CLICKHOUSE_USERNAME=default
CLICKHOUSE_PASSWORD=3LiFNTqs2.7w_
CLICKHOUSE_DATABASE=default

# ── Trigger.dev ──────────────────────────────────────────────────────
# Project ref provided; secret key still pending from user.
# Until the secret key arrives, the chat.agent() worker cannot be deployed
# to Trigger.dev cloud. The Vite dev-server middleware carries the agent
# load against live ClickHouse in the meantime.
TRIGGER_PROJECT_REF=proj_fuxqqqgzdulxvbuthevo
TRIGGER_SECRET_KEY=tr_prod_pnyjPnE1CGusL0CZX36W
# Set to true after a successful `npx trigger.dev@latest deploy`
# (version 20260723.10 deployed with 5 detected tasks)
TRIGGER_WORKER_DEPLOYED=true

# ── Vite browser-exposed (safe to ship) ──────────────────────────────
# Signals to the browser that the agent has a live backend.
VITE_CLICKHOUSE_LIVE=true
VITE_AGENT_ENDPOINT=/api/agent-query
# Trigger.dev token-minting endpoints (the browser POSTs to these to
# get session-scoped PATs; the secret key never leaves the server).
VITE_TRIGGER_PROJECT_REF=proj_fuxqqqgzdulxvbuthevo
VITE_TRIGGER_TOKEN_ENDPOINT=/api/chat-access-token
VITE_TRIGGER_START_ENDPOINT=/api/chat-start
